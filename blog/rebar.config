{erl_opts, [debug_info]}.
{deps, [pcsc]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [blog]}
]}.

{plugins, [
    rebar3_run
]}.

{relx, [{release, {blog, "1.0"},
         [blog]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.
